import {it,expect} from 'vitest';
import {baseline,dimensions,updateMastery,isMastered,nextAction,updateMisconception,type Dimension} from '../../lib/mastery/engine';
const scores:Record<string,number[]>={procedural_only:[.25,1,.2,.35,.3],denominator_misconception:[.25,.8,.3,.2,.25],numerator_only:[.4,.9,.4,.3,.35],understanding_with_slip:[.97,.85,.96,.97,.96],random_guesser:[.2,.5,.2,.5,.5],explanation_with_bad_click:[.95,.5,.97,.97,.97],visual_nonverbal_gap:[.95,.95,.35,.8,.65],corrected_after_intervention:[.97,.95,.96,.97,.98]};
for(const [name,values] of Object.entries(scores))it(`synthetic learner: ${name}`,()=>{let s=baseline();for(let round=0;round<9;round++)for(const [i,d]of dimensions.entries())s=updateMastery(s,{id:`${round}-${d}`,dimension:d as Dimension,score:values[i],reliability:1,source:'synthetic',at:'2026-09-15'});const expected=['understanding_with_slip','explanation_with_bad_click','corrected_after_intervention'].includes(name);expect(isMastered(s)).toBe(expected);if(name==='procedural_only')expect(nextAction(s,[])).toBe('explain');if(name==='corrected_after_intervention'){const code='FRACTION_BIGGER_DENOMINATOR_BIGGER_VALUE';const m=updateMisconception(updateMisconception(undefined,code,.85),code,1,true);expect(isMastered(s,[m])).toBe(true);}});
